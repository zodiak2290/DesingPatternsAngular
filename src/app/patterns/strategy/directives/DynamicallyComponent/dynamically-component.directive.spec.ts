import { DynamicallyComponentDirective } from './dynamically-component.directive';

describe('DynamicallyComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicallyComponentDirective();
    expect(directive).toBeTruthy();
  });
});
