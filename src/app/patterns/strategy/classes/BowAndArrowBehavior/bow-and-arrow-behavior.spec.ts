import { BowAndArrowBehavior } from './bow-and-arrow-behavior';

describe('BowAndArrowBehavior', () => {
  it('should create an instance', () => {
    expect(new BowAndArrowBehavior()).toBeTruthy();
  });
});
